selfish = '01234567'

# selfish = '8'  # we can assign whole new value but it will be warning
# selfish[0] = '8' # we can't reassign part of string mean can't assign new value to specific index
# just can give whole new value
# selfish = selfish + '8'  # we can cocatinate string with old string
print(selfish)
